apiVersion: v1
kind: Secret
metadata:
  name: kimibuilt-secrets
  namespace: kimibuilt
type: Opaque
# NOTE: This secret should be created via kubectl command, not applied directly.
# OPENAI_API_KEY is the gateway key for chat/tool use.
# REMOTE_CLI_CODEX_AGENT_BEARER_TOKEN is used server-side for the preferred
# /api/codex-agent/run plus /events SSE transport. N8N_API_KEY or
# REMOTE_CLI_MCP_BEARER_TOKEN is still accepted for legacy MCP fallback.
# OPENAI_MEDIA_API_KEY is the official OpenAI key for image/video generation.
# PERPLEXITY_API_KEY enables the web-search tool.
# LILLYBUILT_AUTH_* enables the frontend/API login gate.
# DOCKER_HOST is optional and lets docker-exec/code-sandbox target a reachable Docker daemon.
# KIMIBUILT_SSH_* values are optional defaults for the ssh-execute and k3s-deploy tools.
# KIMIBUILT_REMOTE_RUNNER_TOKEN must match the token used by every remote runner.
# If you use KIMIBUILT_SSH_KEY_PATH, the private key file must also be mounted into the backend container.
#
# To create the secret, run:
#   kubectl create secret generic kimibuilt-secrets \
#     --from-literal=OPENAI_API_KEY='your-gateway-api-key-here' \
#     --from-literal=REMOTE_CLI_CODEX_AGENT_BEARER_TOKEN='your-codex-agent-gateway-key-here' \
#     --from-literal=N8N_API_KEY='your-n8n-or-admin-gateway-key-here' \
#     --from-literal=REMOTE_CLI_MCP_BEARER_TOKEN='your-n8n-or-admin-gateway-key-here' \
#     --from-literal=OPENAI_MEDIA_API_KEY='your-official-openai-media-key-here' \
#     --from-literal=PERPLEXITY_API_KEY='your-perplexity-api-key-here' \
#     --from-literal=LILLYBUILT_AUTH_USERNAME='admin' \
#     --from-literal=LILLYBUILT_AUTH_PASSWORD='replace-with-a-strong-password' \
#     --from-literal=LILLYBUILT_JWT_SECRET='replace-with-a-long-random-secret' \
#     --from-literal=DOCKER_HOST='tcp://docker.example.internal:2376' \
#     --from-literal=KIMIBUILT_REMOTE_RUNNER_TOKEN='replace-with-a-long-random-runner-token' \
#     --from-literal=KIMIBUILT_SSH_HOST='10.0.0.5' \
#     --from-literal=KIMIBUILT_SSH_PORT='22' \
#     --from-literal=KIMIBUILT_SSH_USERNAME='ubuntu' \
#     --from-literal=KIMIBUILT_SSH_PASSWORD='your-ssh-password' \
#     --from-literal=KIMIBUILT_SSH_KEY_PATH='/home/kimibuilt/.ssh/id_rsa' \
#     --from-literal=POSTGRES_PASSWORD='choose-a-strong-password' \
#     -n kimibuilt
#
# Or update the existing secret with the additional OPENAI_MEDIA_API_KEY entry.
stringData:
  # Placeholder - this file is for documentation only.
  # The actual secret must be created via kubectl.
  OPENAI_API_KEY: "SET_VIA_KUBECTL_CREATE_SECRET"
  REMOTE_CLI_CODEX_AGENT_BEARER_TOKEN: "SET_VIA_KUBECTL_CREATE_SECRET"
  N8N_API_KEY: "SET_VIA_KUBECTL_CREATE_SECRET"
  REMOTE_CLI_MCP_BEARER_TOKEN: "SET_VIA_KUBECTL_CREATE_SECRET"
  OPENAI_MEDIA_API_KEY: "SET_VIA_KUBECTL_CREATE_SECRET"
  PERPLEXITY_API_KEY: "SET_VIA_KUBECTL_CREATE_SECRET"
  LILLYBUILT_AUTH_USERNAME: "SET_VIA_KUBECTL_CREATE_SECRET"
  LILLYBUILT_AUTH_PASSWORD: "SET_VIA_KUBECTL_CREATE_SECRET"
  LILLYBUILT_JWT_SECRET: "SET_VIA_KUBECTL_CREATE_SECRET"
  DOCKER_HOST: "OPTIONAL_SET_VIA_KUBECTL_CREATE_SECRET"
  KIMIBUILT_REMOTE_RUNNER_TOKEN: "SET_VIA_KUBECTL_CREATE_SECRET"
  KIMIBUILT_SSH_HOST: "OPTIONAL_SET_VIA_KUBECTL_CREATE_SECRET"
  KIMIBUILT_SSH_PORT: "22"
  KIMIBUILT_SSH_USERNAME: "OPTIONAL_SET_VIA_KUBECTL_CREATE_SECRET"
  KIMIBUILT_SSH_PASSWORD: "OPTIONAL_SET_VIA_KUBECTL_CREATE_SECRET"
  KIMIBUILT_SSH_KEY_PATH: "OPTIONAL_SET_VIA_KUBECTL_CREATE_SECRET"
  POSTGRES_PASSWORD: "SET_VIA_KUBECTL_CREATE_SECRET"
