{
  "name": "@lilly/engine",
  "version": "0.7.0",
  "private": true,
  "description": "Lilly-owned browser game engine contracts and runtime",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.browser.json"
  }
}
